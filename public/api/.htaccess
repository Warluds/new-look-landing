# Внутри /api не применяем SPA-редиректы — отдаём файлы как есть
<IfModule mod_rewrite.c>
  RewriteEngine Off
</IfModule>

<IfModule mod_headers.c>
  Header set Cache-Control "no-store, no-cache, must-revalidate"
</IfModule>
